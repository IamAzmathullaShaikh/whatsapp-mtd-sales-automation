# Python environment files
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# Freebuff app data
.freebuff/

# Private Data & Sales Dumps (CRITICAL)
*.xlsx
*.csv
!template_party_master.xlsx
!template_sales_dump.xlsx
!user_settings.json

# Generated Logs and Exports
logs/
exports/
custom_groups.json
PyWhatKit_DB.txt
user_settings.json

# WhatsApp Web persistent browser profile (holds your login session)
.whatsapp_web_profile/
.venv/

# Multi-company data (per-company settings + active pointer; sample.json.example is committed)
companies/*.json
companies/active.txt

# Release build artifacts (built locally; CI produces the official packages)
dist/
build/

# Upstream skill clones are committed snapshots (skills/sources/ and
# .agents/skills/.upstream/). `skilllib sync` / update.sh re-clone them fresh
# when a .git dir is missing — see scripts/sync.py.
